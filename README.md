# Morning Briefing Agent

A Python agent that delivers your daily morning briefing.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate   # Mac/Linux
pip install -r requirements.txt
```

Create a `.env` file for your API keys (never commit it — it is listed in `.gitignore`).

## Run

```bash
python agent.py
```
